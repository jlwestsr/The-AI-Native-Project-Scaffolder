# GEMINI Project Context for {{ project_name }}

## Project Overview
This project is generated by Forge.

## Technical Stack
- **Language**: Python {{ python_version }}+
- **Project Type**: {{ profile|upper }} Architecture
{% if profile == "web" %}
- **Backend**: FastAPI (Python)
- **Frontend**: HTML/JS/CSS (Jinja2)
{% elif profile == "mvc" %}
- **Architecture**: MVC (Model-View-Controller)
{% elif profile == "system" %}
- **Focus**: Ansible / Automation
{% endif %}

## Agent Instructions
- Always work on the `develop` branch.
- Follow the directives in `AI_DIRECTIVES.md` (includes strict behavior mandates).
- The main entry point depends on the profile:
{% if profile == "mvc" %}
  - `src/app/main.py`
{% elif profile == "web" %}
  - `src/backend/app/main.py` (or similar)
{% else %}
  - Check `src/` or `scripts/`.
{% endif %}

## Project Influences
- **Forge**: The tool that created this scaffold.
