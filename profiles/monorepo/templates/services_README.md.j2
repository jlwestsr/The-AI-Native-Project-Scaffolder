# services/

**Class: SHIPS** — shared platform services used across products
(APIs, data stores, retrieval, graph, gateways, …).

Add `services/<name>/` with clear ownership and docs under `docs/ops/` for
runbooks.
