# example-app

Placeholder product under **{{ project_name }}**.

## Intent

Demonstrate the `products/<name>/` convention. Replace this tree with a real
application or delete it once you have a first product.

## Next steps

1. Rename directory to your product name.  
2. Add package code, tests, and CI as needed.  
3. Register the tree in `config/workspace-layout.yaml` under `trees:`.  
